name: Release Manual HCU Wheels

permissions:
  contents: read

on:
  # ---- 临时 PR 触发，仅用于测试构建流程，测试完删除 ----
  pull_request:

  # ---- 手动触发，可选任意 ref (分支 / tag / commit SHA) ----
  workflow_dispatch:
    inputs:
      ref:
        description: 'Git ref (branch, tag, or commit SHA) to build. Empty = default branch'
        required: false
        default: ''
        type: string
      torch_version:
        description: 'PyTorch version to install'
        required: false
        default: '2.10.0'
        type: string
      skip_gateway:
        description: 'Skip sgl-model-gateway build'
        required: false
        default: false
        type: boolean

jobs:
  compile:
    # ---- 并发控制: 同一 ref 的 job 只保留最新一个 ----
    concurrency:
      group: release-manual-hcu-${{ inputs.ref || github.sha }}
      cancel-in-progress: true
    # ---- 自托管 HCU runner，在 Docker 容器内执行 ----
    runs-on: [self-hosted, hcu]
    timeout-minutes: 180

    container:
      image: ${{ vars.HCU_CI_RELEASE_IMAGE }}
      options: >
        --device=/dev/kfd --device=/dev/dri/ --group-add video
        -v /opt/hyhal/:/opt/hyhal/:ro
        --shm-size=80G
        --cap-add=SYS_PTRACE
        --security-opt seccomp=unconfined
        --privileged=true

    strategy:
      matrix:
        python-version: ['3.10']

    env:
      TORCH_VERSION: ${{ inputs.torch_version || '2.10.0' }}
      SKIP_GATEWAY: ${{ inputs.skip_gateway || false }}
      PIP_INDEX_URL: http://10.16.1.201:9929/nightly/dtk2604/+simple/
      PIP_TRUSTED_HOST: 10.16.1.201
      RESOURCE_SERVER: http://10.16.1.201:8000/
      HCU_RUNNER_HOST_UID: ${{ vars.HCU_RUNNER_HOST_UID || '1000' }}
      HCU_RUNNER_HOST_GID: ${{ vars.HCU_RUNNER_HOST_GID || '1000' }}
      CHECKOUT_REF: ${{ inputs.ref || github.sha }}

    steps:
      # =======================================================================
      # 环境准备
      # =======================================================================

      - name: Normalize workspace ownership before checkout
        run: |
          chown -R "${HCU_RUNNER_HOST_UID}:${HCU_RUNNER_HOST_GID}" "${GITHUB_WORKSPACE}" || true

      - uses: actions/checkout@v4
        with:
          ref: ${{ env.CHECKOUT_REF }}
          submodules: 'recursive'

      - name: Resolve wheel commit SHA
        run: |
          WHEEL_COMMIT_SHA="$(git rev-parse HEAD)"
          echo "WHEEL_COMMIT_SHA=${WHEEL_COMMIT_SHA}" >> "${GITHUB_ENV}"
          echo "Building HCU wheels for ${WHEEL_COMMIT_SHA}"

      # 容器内已预装 Python 3.10 + DTK/ROCm + gcc + cmake
      - name: Install build deps
        run: |
          pip install torch==${TORCH_VERSION} build scikit-build-core cmake ninja wheel setuptools setuptools-scm setuptools-rust maturin ciupload
          wget ${RESOURCE_SERVER}/Jenkins/CompileDep/sglang/protoc-24.4-linux-x86_64.zip
          unzip protoc-24.4-linux-x86_64.zip -d /usr/local/
          chmod +x /usr/local/bin/protoc
          ln -s /usr/local/bin/protoc /usr/bin/protoc
          wget ${RESOURCE_SERVER}/Jenkins/CompileDep/sglang/rustup-init.sh
          export RUSTUP_DIST_SERVER=https://mirrors.tuna.tsinghua.edu.cn/rustup
          export RUSTUP_UPDATE_ROOT=https://mirrors.tuna.tsinghua.edu.cn/rustup/rustup
          bash rustup-init.sh -y
          rm -rf ${HOME}/.cargo/config
          mkdir -p ${HOME}/.cargo
          cat > ${HOME}/.cargo/config << 'EOF'
          [source.crates-io]
          replace-with = 'rsproxy-sparse'

          [source.tuna-sparse]
          registry = "sparse+https://mirrors.tuna.tsinghua.edu.cn/crates.io-index/"

          [source.rsproxy-sparse]
          registry = "sparse+https://rsproxy.cn/index/"

          [net]
          retry = 5
          git-fetch-with-cli = true

          [http]
          low-speed-limit = 1
          EOF

      - name: Install DTK
        run: |
          DTK_PKG_URL=${RESOURCE_SERVER}/dtk-pkg/dtk26.04/DTK-26.04-rc4-centos8-x86_64.tar.gz
          cd /opt
          wget -q $DTK_PKG_URL
          tar -xzf $(basename $DTK_PKG_URL)
          mv dtk-* dtk

      # =======================================================================
      # 编译阶段: 手动触发, 忽略路径检测, 全量编译
      # =======================================================================

      - name: Build sgl-kernel (HIP)
        run: |
          source /opt/dtk/env.sh
          cd python/sglang/kernels/aot && python setup_hip.py bdist_wheel
          echo "=== sgl-kernel wheel ==="
          ls -lh dist/*.whl

      - name: Build sglang python package
        env:
          SETUPTOOLS_SCM_PRETEND_VERSION: "0.5.15.post1"
        run: |
          source /opt/dtk/env.sh
          source $HOME/.cargo/env
          cd python
          export SETUPTOOLS_SCM_PRETEND_VERSION="${SETUPTOOLS_SCM_PRETEND_VERSION}"
          python -m build --wheel --no-isolation -Cfeatures=all_hip
          echo "=== sglang wheel ==="
          ls -lh dist/*.whl

      - name: Build sgl-model-gateway
        if: ${{ env.SKIP_GATEWAY != 'true' }}
        run: |
          source /opt/dtk/env.sh
          source $HOME/.cargo/env
          export RUSTUP_DIST_SERVER=https://mirrors.ustc.edu.cn/rust-static
          export RUSTUP_UPDATE_ROOT=https://mirrors.ustc.edu.cn/rust-static/rustup
          cd sgl-model-gateway && cargo build --release
          cd bindings/python && maturin build --release --skip-auditwheel
          echo "=== sgl-model-gateway wheel ==="
          ls -lh target/wheels/*.whl

      # =======================================================================
      # 修复 (auditwheel) & 上传产物
      # =======================================================================

      - name: Apply DAS local version to wheels
        run: |
          source /opt/dtk/env.sh
          DTK_PKG_URL=${RESOURCE_SERVER}/dtk-pkg/dtk26.04/DTK-26.04-rc4-centos8-x86_64.tar.gz
          DTK_VER=$(echo $DTK_PKG_URL | grep -oP 'dtk\K[0-9]+(\.[0-9]+)+' | tr -d '.')
          mkdir -p output
          shopt -s nullglob
          wheels=(python/sglang/kernels/aot/dist/*.whl python/dist/*.whl sgl-model-gateway/bindings/python/target/wheels/*.whl)
          if (( ${#wheels[@]} == 0 )); then
            echo "No HCU wheels were built" >&2
            exit 1
          fi
          CIUpload REPAIR \
            --dtk_version "${DTK_VER}" \
            --torch_version "${TORCH_VERSION}" \
            --git_commit "${WHEEL_COMMIT_SHA}" \
            --outputdir output \
            -f "${wheels[@]}"
          echo "=== Repaired wheels ==="
          ls -lh output/

      - name: Upload wheel artifact
        uses: actions/upload-artifact@v4
        with:
          name: hcu-wheels-${{ github.run_id }}
          path: output/*.whl
          retention-days: 30

      - name: Normalize workspace ownership
        if: always()
        run: |
          chown -R "${HCU_RUNNER_HOST_UID}:${HCU_RUNNER_HOST_GID}" "${GITHUB_WORKSPACE}" || true
